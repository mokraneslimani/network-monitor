services:
  network-monitoring:
    build: .
    container_name: network-monitoring
    network_mode: "host"
    cap_add:
      - NET_ADMIN
      - NET_RAW
    volumes:
      - ./data:/app/data
    restart: unless-stopped
